import AuthModel from "app/primary-model/auth.model";
import * as React from "react";

interface ISignUpProps {
  model: AuthModel;
}

const SignUp: React.FunctionComponent<ISignUpProps> = (props) => {
  return <div className=""></div>;
};

export default SignUp;
